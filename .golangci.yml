version: "2"
linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - errcheck
    - gosec
  exclusions:
    rules:
      - path: "_test\\.go"
        linters: [gosec]
formatters:
  enable:
    - gofmt
