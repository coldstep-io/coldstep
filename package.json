{
  "name": "coldstep-action",
  "description": "Node20 GitHub Action bundles for coldstep pre/main/post lifecycle.",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/pre.ts --bundle --platform=node --target=node24 --format=cjs --sourcemap --outfile=dist/pre/index.js && esbuild src/main.ts --bundle --platform=node --target=node24 --format=cjs --sourcemap --outfile=dist/main/index.js && esbuild src/post.ts --bundle --platform=node --target=node24 --format=cjs --sourcemap --outfile=dist/post/index.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "//overrides": "@actions/github and its transitive @actions/http-client both declare 'undici ^6.23.0'. The floor here is bumped one patch to clear advisories present in 6.23.0. Re-audit and bump this floor whenever 'npm audit' surfaces new GHSA entries against undici 6.x; on a major undici release, retest before widening the range.",
  "overrides": {
    "undici": "^6.23.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
