# Changelog

All notable changes to ColdStep are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

## [0.5.2] — 2026-06-09

### Added

- **io_uring WRITE/WRITEV egress visibility (ORDER 2 / Phase 3.3).** `IORING_OP_WRITE` (23) and `IORING_OP_WRITEV` (2) submissions are now surfaced as `io_uring_send` events when their fd resolves to an AF_INET socket. The fd is walked `req->file` → socket → `sk` with an `S_ISSOCK` guard before `file->private_data` is trusted, so routine file I/O bails with no event (no ringbuf flood). The connected-peer dst (`dst_ip`/`dst_port`) is populated from that same walk for every write-class opcode, reusing the ORDER 1 resolver. Native-IPv6 io_uring writes are not surfaced (the walk gates on AF_INET, matching the IPv4 focus of the LSM/cgroup egress paths).
- **IPv6 LSM defense-in-depth twin (ORDER 4 / Phase 4.2).** The BPF LSM `socket_connect` / `socket_sendmsg` hooks now enforce native IPv6 (previously IPv4-only), mirroring the cgroup `connect6`/`sendmsg6` path: they read the 16-byte destination (from `sockaddr_in6` or the connected `skc_v6_daddr` fallback) and gate it against the `allowed_ipv6` LPM trie via `lsm_v6_enforce`, with `::1` (loopback) and `fe80::/10` (link-local) always bypassing and IPv4-mapped destinations judged against the IPv4 allowlist. Denies emit an `AF_INET6` deny event. Like every LSM hook it enforces only where `bpf` is in the kernel `lsm=` boot chain; the load+attach path verifies on `CONFIG_BPF_LSM=y` kernels.
- **Kernel-truth executable identity (Sub-project C / ORDER 5 / Phase 5.1).** `exec` JSONL events now carry `exe_inode` + `exe_dev`, read in-kernel from `mm->exe_file->f_inode` at `sched_process_exec`, plus a best-effort userspace `exe_sha256` content hash. Unlike the spoofable `comm` / `exe` path, the inode/dev pair uniquely identifies the on-disk binary: replacing it (new inode at the same path) changes `exe_inode`, a rename keeps it. `0` means the walk failed (older kernel / no mm). The content hash is TOCTOU-best-effort (absolute paths only, ≤16 MiB, empty on any error) — a tamper hint layered on the robust kernel identity, not a guarantee the hashed bytes are the exec'd bytes.

### Changed

- **Internal: ring-reader scaffolding dedup complete.** All 17 enrichment ringbuf readers (connect/tls/udp/http and the rest) now share the `runRingReader` loop extracted in 0.5.1; `readDenyRing` stays separate by design (burst-drain + `SetDeadline` protocol). No behavior change.

## [0.5.1] — 2026-06-09

### Added

- **io_uring TLS destination correlation (ORDER 1 / BG-5).** `io_uring_tls` events now carry the connected IPv4 peer (`dst` + `dst_port`), resolved in kernel from the submission's socket (`req->file` → socket → `sk` → `skc_daddr`), instead of the `"unknown"` placeholder. Best-effort: the SNI still emits with `dst:"unknown"` when the peer is not resolvable.
- **IPv6-literal allowlisting (SP-2).** `allow` / `allow-file` now accept native IPv6 literals (`2001:db8::1`) and IPv6 CIDRs (`2001:db8::/32`). They are classified to the IP path, parsed by `policy.Parse` into the v6 buckets, and programmed into the defend `allowed_ipv6` LPM trie (literals as `/128`, CIDRs as prefix entries) alongside AAAA-resolved domains. Previously IPv6 literals were silently treated as hostnames and failed to resolve. The `!CIDR` ignore mechanism remains IPv4-only.
- **Grouped suggested-allow artifact (SP-3).** The post step writes `.coldstep-suggested-allow.txt` — a paste-ready, grouped allow-file (hostnames preferred, then bare IPs) — alongside the existing `suggested-allow` output and summary block.

### Fixed

- **Stop step no longer drops the report on a transient GitHub API failure.** The post step renders the report from the already-downloaded + SHA-verified cached binary (`cachedColdstepBinaryPath`) instead of re-fetching the release SHA over the network; a transient Releases API rate-limit/5xx no longer silently omits the report.

### Changed

- **Internal: ring-reader scaffolding dedup.** Extracted the shared ringbuf read loop (backoff / ErrClosed / ctx-cancel) into `runRingReader`; migrated the exec/fork/fs readers. No behavior change.

## [0.5.0] — 2026-06-08

### Changed (BREAKING)

- **Single combined `coldstep` binary.** The agent (`cmd/coldstep`) and the composite action helper (the former `cmd/coldstep-action`, now `internal/actioncli`) are merged into one binary that dispatches `run | validate | start | stop | diff | assert-integrity`. The Release ships one artifact and consumers download that prebuilt build (they no longer compile source on the runner). `scripts/build-agent-linux.sh` builds one binary; CI/supply-chain reference `bin/coldstep <subcommand>`.
- **Reporting fully consolidated to one userspace path.** The agent writes data only (`.coldstep-events.jsonl`); the `.coldstep-detect.md` agent digest and the `COLDSTEP_DETECT_LOG` / `config.DetectLogPath` plumbing are removed. `coldstep stop` renders both reports from the JSONL via `internal/report/markdown` (`RenderSimple` → job summary, `RenderDetailed` → `.coldstep-report.md`). This fixes a regression where `report: pr-comment` / `both` posted nothing once the agent stopped writing the digest.
- **Allowlist input consolidation.** Removed the `ignored-nets`, `ignored-nets-file`, and `bootstrap-allowlist` action inputs. The allowlist is now a single model: `allow` / `allow-file` carry domains, wildcards, IPv4 literals/CIDRs, and `!CIDR` ignore entries (the only ignore mechanism); `no-default-ignored-nets` still toggles the implicit RFC1918 ignores. Migration: `ignored-nets: "10.0.0.0/8"` → `allow: "!10.0.0.0/8"` (or `!CIDR` lines in an `allow-file`); `bootstrap-allowlist: true` → copy the reference packs in `scripts/coldstep_bootstrap/` into your own `allow-file`. If a removed input is still set, the action emits a one-line warning naming the replacement rather than silently ignoring it.

### Removed

- **Reputation / threat-intel enrichment subsystem (OTX + rDNS).** Removed `internal/reputation/*` (the pluggable `Enricher` interface, registry, loader, and the AlienVault OTX backend), `internal/report/enrich/*` (the Source pipeline), and the `coldstep-report` `otx-enrich` / `rdns-enrich` subcommands. The report model drops its `otx` and `rdns` slots and the `ClassifiedIndicator.rdns` field. The `OTX_API_KEY` / `COLDSTEP_OTX_API_KEY` secrets and the CI enrichment steps are no longer used. This slims the post-run report pipeline to build/diff/render only; detect reports no longer perform any outbound threat-intel lookups.

### Fixed

- **Defend run mislabeled as `detect` in the report.** The report inferred run mode only from `deny` events, so a defend run where everything was allowlisted (zero denies — the success case) rendered "detect ✅ no anomalies". Mode is now carried in the shutdown `meta` event and read authoritatively.
- **Markdown link-injection in report table cells.** `mdCell` neutralized `|`, backtick, newline, and `<` but left `[`/`]`, so a hostile SNI/FQDN could render a clickable link in the Job Summary; the brackets are now neutralized too.

---

## [v0.4.1] — 2026-05-30

### Security

- **DNS cache trust — defend-mode poisoning fix (#237).** The defend allow-path fallback (`dst_is_allowlisted`: `dns_cache[ip] -> owner -> allowed_domains[owner]`) was fed from DNS replies **sniffed off runner traffic**. A hostile build step could forge a reply mapping an allowlisted FQDN to an attacker IP, poison the BPF enforcement map, and egress to that IP past the allowlist. The sniffed cache now feeds detection enrichment only; the defend enforcement map is seeded from the agent's own resolver via `policy.ResolveOwners` at startup and refreshed on each DNS-drift tick, raising the runtime fallback trust to match the startup allowlist-compile trust.

### Added

- **Pure-Markdown report generator (#238).** New `internal/report/markdown` package reads `.coldstep-events.jsonl` and renders a simple (job-summary) and detailed (artifact) report with zero embedded HTML. Phase 0 of consolidating the separate `coldstep-report` binary; unused until later phases wire it in.

### Fixed

- **Cross-layer deny dedup; sendpage load detection (#237).** One blocked syscall fires both the cgroup and LSM hook on LSM-enabled kernels, double-counting denies. Denies are now de-duplicated across hook families within a 1s window (same-family repeats still emit); the suppressed twin is tallied as corroboration.
- **Signing key not trimmed; per-call markdown regexes (#239).** `COLDSTEP_SIGNING_KEY` was the only `LoadFromEnv` env read not `TrimSpace`'d — a CI-injected trailing newline crashed agent startup (NewSigner treats bad base64 as fatal). `sanitizeDigestForMarkdown` recompiled two fence regexes per call; hoisted to package scope.
- **Digest replay accepts legacy `enforce` alias (#232).** `isBlockingDigestMode` treats `enforce` / `enforce+<backend>` as `defend` so digests replayed from pre-rename JSONL still surface the defend triage row, allowlist-trust section, and IPv6-defend logic.
- **Enrichment model canonical JSON encoder (#228).** Reputation enrichment writes the model with the canonical encoder so signatures and downstream diffs stay stable.
- **Report BOM strip, fingerprint conflation, preamble HTML (#230).**
- **Action pidfile path; track agent PID not sudo PID (#229).**
- **Distinguish DNS attempt timeout from cancel; drain enrich resCh (#227).**
- **Reject wildcard allow-list entries in defend mode (#226).** Wildcards are classify-only and not programmed into the exact-match BPF `allowed_domains` map; defend now rejects them explicitly instead of silently ignoring.
- **Downgrade `defend+lsm` label when LSM hooks attach but stay silent (#225).**
- **Fall back to cgroup-only on non-sendpage LSM load failure (#224).**
- **Poll agent exit instead of fixed 400ms sleep in runStop (#223).**
- **Treat empty baseline as unavailable in BuildDiff (#222).**

### Changed

- **Publish `coldstep-report-linux-amd64` as a release asset (#219, #220).**
- **dist-up-to-date CI gate, tighter golangci exclusions, docs (#221).**
- **Dependency bumps:** `golang.org/x/sys` 0.44.0 → 0.45.0 (#233), `golangci/golangci-lint-action` 8 → 9 (#234), `actions/setup-go` 5 → 6 (#235), `@types/node` 25.8.0 → 25.9.1 (#236).

---

## [v0.4.0] — 2026-05-21

### Added
- **H14 — IPv6 block phase 2 (coverage telemetry + docs + bypass test) (#206).** Completes the IPv6 defend story whose BPF + userspace machinery (cgroup `connect6`/`sendmsg6` against an `allowed_ipv6` LPM trie populated from parallel AAAA lookups) landed in P2-1 (v0.2.6, #164). Telemetry: `CoverageReport.IPv6` changes from `bool` to a string enum with three named values (`telemetry.CoverageIPv6Off` / `telemetry.CoverageIPv6ObserveOnly` / `telemetry.CoverageIPv6Enforce`); the field stays in the same JSON position (`"ipv6": …`, no `omitempty`) but consumers that parsed the prior `bool false` stub need to switch to a string-typed reader. `buildCoverageReport` grows an `ipv6Enforced bool` parameter; the agent computes `ipv6Enforced := cfg.Mode == config.ModeDefend && hasDefend`. An empty `allowed_ipv6` trie is still reported as `"enforce"` because the cgroup6 hook denies every non-loopback IPv6 destination in that posture (the existing digest renderer already labels it `✓ gated (defend block-all — empty allowed_ipv6)`). New `internal/policy/ipv6_bypass.go` mirrors the BPF helpers `cg_ipv6_is_loopback` / `cg_ipv6_is_link_local` as a regression anchor (`IPv6BypassesDefend(net.IP) bool`); production code does not consult it (the BPF program is authoritative) but a table-test pins the exact classification — `::1` and `fe80::/10` bypass; the adjacent `fec0::/10` / `fe00::/10`, unique-local `fc00::/7`, multicast `ff00::/8`, global `2000::/3`, and the unspecified `::` do not. README "At a glance" defend row now reads "Block IPv4 and IPv6 egress not on the allowlist (cgroup `connect4`/`sendmsg4` + `connect6`/`sendmsg6`; QUIC payloads remain uninspected)"; `SECURITY.md` Coverage Boundaries splits the old `IPv6 (all)` row into `IPv6 TCP (connect6)` and `IPv6 UDP (sendmsg6)` and replaces the "Why IPv4-only" paragraph with a "Defend-mode IPv6 enforcement (H14, v0.4.0)" section.
- **H15 — `lsm/io_uring_cmd` defend hook (#212).** New `SEC("lsm/io_uring_cmd")` program in `bpf/trace_lsm_defend_iouring.inc` denies non-allowlisted IPv4 egress on `IORING_OP_URING_CMD` submissions in **defend** mode — closing a narrow gap where socket-backed `URING_CMD` requests reach the network stack via `file->private_data` without flowing through `security_socket_sendmsg()` (which `lsm/socket_sendmsg` already covers for `IORING_OP_SEND` / `IORING_OP_SENDMSG`). Reads `ioucmd->file`, treats `file->private_data` as `struct socket *`, verifies `sk->__sk_common.skc_family == AF_INET`, then runs the same LPM allowlist + DNS-cache + ignored-CIDR policy used by `lsm_socket_sendmsg`; returns `-EPERM` on deny via the existing `lsm_deny_events` ringbuf. Every `bpf_probe_read_kernel` return is checked; `daddr == 0` falls through to allow so non-AF_INET URING_CMD submissions (NVMe pass-through, etc.) are unaffected. Defense-in-depth only — the cgroup `connect4` / `sendmsg4` hooks remain the always-on primary IPv4 defense path. Graceful degradation: `defend.HaveIOUringLSM()` probes kernel BTF for the `bpf_lsm_io_uring_cmd` LSM hook (not `security_uring_cmd`, which is present on any 5.19+ kernel regardless of `CONFIG_BPF_LSM` — verified through a follow-up fix that matches the existing `sendpage` probe pattern). `LoadDefendObjectsForKernel(obj, wantLSM, wantIOUringLSM bool)` strips the io_uring program from the spec when the kernel cannot host it; a `BPFStatus` row named `lsm/io_uring_cmd` is appended **only** when attach was attempted so pre-5.19 kernels are not reported as having a degraded hook they cannot host.
- **H16 — DNS allowlist drift watchdog (#205).** Background goroutine periodically re-resolves the startup allowlist and emits a `dns_drift` JSONL event when IPv4 addresses change relative to the snapshot programmed into the BPF map. **Warning-only by design**: the live BPF enforce policy is intentionally **not** updated mid-run — adding freshly-resolved CDN tenant IPs to the LPM trie between the lookup and the egress attempt is a TOCTOU risk (per the H16 risk note in the hardening plan). The snapshot remains the source of truth for enforcement; the goroutine's job is purely advisory so operators of long-running jobs see CDN tenant or LB pool rotation as a digest warning instead of a silent gap. New `policy.DriftReport` struct (`AddedIPs`, `RemovedIPs`, `CheckedAt`); pure `policy.Diff(original, updated CompileResult) DriftReport` returning sorted IPv4 set differences (IPv6 deferred); `policy.ReResolve(ctx, original, resolver, maxAttempts)` re-runs `CompileDomainAllowlist` against the original domain list, sharing the existing concurrency budget (`coldstepDomainLookupConcurrencyLimit=32`, `coldstepDomainLookupAttemptTimeout=25s`); periodic check uses `maxAttempts=1` so transient resolver flakes recover on the next tick rather than producing retry storms. New `telemetry.DNSDriftEvent` (json type `"dns_drift"`, fields `ts` / `added_ips` / `removed_ips` / `domain_count` / `checked_at`) signed in the same canonical-marshal path as every other event type. New portable `internal/agent/dns_drift.go` `runDNSDriftWatch(...)` ticks every `allowlistReCheckInterval` (5 minutes, mirroring the H9 staleness threshold); the agent launches it inside `Run`'s wait group when `defendCompiled.Domains` is non-empty. `DigestInput.DNSDriftCount` surfaces the total; `writeAllowlistTrust` emits a fourth advisory box when non-zero: `> ⚠️ **DNS drift detected** — allowlist IPs changed N time(s) during this run. Enforcement was not updated mid-job.`
- **H17 — Learning-mode poisoning protections — DGA heuristics, observation window gate, diff flag (#207).** Per-domain risk metadata + reviewer-facing surface around the existing detect-mode poisoning gates (`--min-observation-hours`, `diff --fail-on-new-domain`). `model.SuspiciousDomain` grows three H17 fields: `ObservationCount int` (canonical name for downstream consumers; legacy `Occurrences` stays on the wire for backward compatibility), `FirstSeenTS time.Time` (earliest event timestamp; lets reviewers sort by recency), and `RiskHint string` (one of `suspicious-dga`, `single-observation`, or empty — DGA wins when both fire). New exported `model.HasHighEntropyLabel(domain string) bool` returns true when the leftmost label is `[0-9a-f]{16,}` OR (length ≥ 12 AND Shannon entropy ≥ 3.5 bits/char); IP literals and empty input return false. The existing `high_entropy` reason keeps its looser 8-char floor so borderline entries still surface; the stricter 12-char `RiskHint` floor only fires the headline `suspicious-dga` tag. `cmd/coldstep-report/build_model.go` `--min-observation-hours` now emits the H17-spec stderr line alongside the existing `::warning` annotation; architecture is unchanged (build-model writes the model with `short_observation_window=true`; the hard fail still lives in `assert-integrity` so the model JSON remains inspectable on failure). New `reportLearningModeReviewerHints` helper in `assert-integrity` emits a warn-only `::warning title=Coldstep learning-mode reviewer hints (H17)::N DGA-shaped / M single-observation destination(s)` annotation followed by per-domain rows — integrity verdict is **not** changed; hard fails remain `short_observation_window`, BPF tamper, and the existing required-types / canary checks. New `QUICK_START.md` "Building a safe allowlist (H17 learning-mode poisoning protections)" subsection documents the `risk_hint` taxonomy and the recommended ≥ 24h window + baseline diff + manual review bar.
- **H19 — QUIC/HTTP3 observation heuristic (#208).** UDP egress on dport 443 now carries a per-event `possible_quic bool` (json `possible_quic,omitempty`) on `UDPEvent` and feeds a per-run `CoverageReport.QuicObserved uint64` (json `quic_observed,omitempty`) — surfacing the visibility gap in the digest coverage scope table and the technical-details fold without requiring QUIC crypto decryption (which is structurally outside what an eBPF + userspace correlator can do without a full QUIC stack). `readUDPRing` sets `ev.PossibleQUIC = port == 443` and calls `stats.addQUICObserved()` for each true case; decoupled from the older `IsQUICCandidate` predicate (which keeps the non-loopback-IPv4 gate for the `quic_candidate` JSONL emitter) so the H19 counter mirrors exactly the per-event flag. `quicCoverageCell` now renders `⚠️ QUIC/HTTP3 (UDP 443) — N events observed (heuristic, not enforced)` when `QuicObservedCount > 0` (falling back to the older candidate cell when only `QUICCandidateCount` is populated). Both new JSON fields are `omitempty`: a run without UDP/443 egress leaves every existing udp JSONL line byte-identical and the coverage `quic_observed` field absent (rather than serializing `:0`). `SECURITY.md` Coverage Boundaries row for QUIC/HTTP3 flips from "UDP event only" to "heuristic observation only (UDP port 443 flagged as `possible-quic`; not enforced)".
- **H20 — Red-team validation CI (12 attack-path integration tests) (#209).** New `internal/agent/redteam_test.go` (`//go:build integration && linux && !windows`) with 12 `TestRedTeam_*` integration tests, one per attack path from the v0.4.0 security review. Each stands up the agent in-process via `Run(ctx, cfg)`, performs a triggering action (`net.Dial`, raw `python3 SOCK_DGRAM sendto`, `curl --http1.1`, `os/exec`, etc.), and polls `.coldstep-events.jsonl` for the expected event row (or asserts absence in pass-through cases): detect-mode TCP/UDP/DNS-as-UDP53/TLS-SNI/exec coverage (5 tests), defend-mode block + allow + loopback + domain-resolution checks (4 tests), plus 3 `t.Skip` stubs for H7 IPv6 telemetry, H19 UDP `possible_quic` JSONL, and per-SQE `io_uring_send` JSONL emit (the suffixes `_RequiresH7` / `_RequiresH19` / `_RequiresH8` are intentionally explicit so a follow-up PR knows what to unskip — and H19's stub is the next to land). New `redteam-integration` job in `.github/workflows/coldstep-redteam-ebpf.yml` (`ubuntu-latest`, `timeout-minutes: 20`) runs `scripts/build-agent-linux.sh` then `sudo env "PATH=$PATH" "HOME=$HOME" go test -tags=integration ./internal/agent/... -run '^TestRedTeam_' -count=1 -parallel 1 -timeout 15m -v`. The existing `audit-validation` job is left untouched.

### Fixed
- **DNS drift watchdog races with agent shutdown (#215).** `runDNSDriftWatch` now checks `ctx.Err()` immediately after `policy.ReResolve` returns. The re-resolve call holds `coldstepDomainLookupAttemptTimeout` (25s) of in-flight DNS lookups using its own bounded context; on agent shutdown the per-lookup context cancels and `ReResolve` returns a partial / empty `CompileResult` that diffs against the original snapshot as `RemovedIPs=[all]`, producing a spurious `dns_drift` event in `.coldstep-events.jsonl` even though the agent was on its way out. The new `ctx.Err()` guard short-circuits the comparison so shutdown can't surface as drift.
- **`.coldstep-telemetry.json` missing TCP-state / QUIC-observed / DNS-drift counters (#216).** Three counter groups already fed the shutdown digest via `DigestInput` but were missing from the on-disk `telemetry.Summary` that produces `.coldstep-telemetry.json`. Downstream consumers (report tooling, dashboards) had no machine-readable twin of the digest's TCP-handshake / QUIC heuristic / DNS-drift cells and had to reparse the JSONL stream to recover them. `snapshotSummary` now plumbs through: `tcp_state_total` / `tcp_state_confirmed` / `tcp_state_refused` / `tcp_state_ringbuf_reserve_failures` (kernel-confirmed TCP handshake transitions — P3-2b, originally landed v0.2.8), `quic_observed` (H19), and `dns_drift_observations` (H16). All new fields are `omitempty` with zero defaults so existing detect-mode runs that don't exercise these paths see no new noise fields in the summary file.

---

## [v0.3.0] — 2026-05-20

### Added
- **H7**: IPv6 egress observe-only hooks — `cgroup/connect6` + `cgroup/sendmsg6` emit `tcp6`/`udp6` JSONL events with `note: "ipv6-not-enforced"`. Digest ⚠️ box when IPv6 egress detected. `CoverageReport.ipv6` transitions to `"observe-only"`.
- **H8**: TLS SNI confidence scoring — `TLSConfidence` enum (`full`/`partial`/`inferred`/`unknown`) on `TLSEvent`. Digest KPI row; badge downgrades to ⚠️ when partial+unknown>0.
- **H9**: DNS TTL staleness warning — digest ⚠️ when allowlist compiled >5 min ago.
- **H9**: Wildcard CDN risk scoring — flags `*.githubusercontent.com`, `*.s3.amazonaws.com`, `*.blob.core.windows.net`, `*.azureedge.net`, `*.cloudfront.net`; `slog.Warn` per domain; included in JSONL meta event.
- **H10**: Per-domain observation counts — agent tracks `dst_domain → connection_count`; digest lists zero-contact allowlist entries as trimming candidates.
- **H11**: Summary file integrity — SHA-256 of `.coldstep-events.jsonl` in `MetaEvent.EventsFileSHA256`; `<!-- coldstep-digest-sha256: <hex> -->` appended to digest Markdown.
- **H12**: Allowlist startup entry count — `MetaEvent.AllowlistEntryCount` records LPM trie size at startup.
- **H13**: Docker-in-Docker detection — reads `/proc/1/cgroup`; emits `MetaEvent.RunnerEnv` (`"dind"`/`"standard"`/`"unknown"`); digest ⚠️ box when DinD detected.

### Audited
- **H18**: eBPF safety audit (#204) — every BPF C source under `bpf/` annotated inline against the Section 5 checklist: 5a (map lookup null checks), 5b (ringbuf reserve/discard pairing on every exit path), 5c (pointer arithmetic bounds), 5d (loop bounds — `#pragma unroll` sendmmsg walk capped at `SENDMMSG_EXTRA_MAX = 7`), 5e (BTF CO-RE field stability), 5f (helper return checks / zero-init), 5g (cgroup attach cleanup on partial-attach failure), 5h (allowlist startup entry count log — wired by H12). **No safety gaps found**; no behavioural BPF changes — comments only. Two superseded standalone defend translation units (`trace_defend.bpf.c`, `trace_lsm_defend.bpf.c`) documented as dead code; active path remains `trace_defend_all.bpf.c` with the included `.inc` files.

---

## [v0.2.9] — 2026-05-20

### Added
- **H1 — Honest detect-digest verdict + Coverage scope table (#193).** Headline badge text now spells out what each emoji means so operators don't mistake ✅ for "every byte of egress observed": ✅ **No anomalies detected (IPv4 TCP/UDP in scope)**, ⚠️ **Partial observation or coverage gaps — review required**, 🚨 **BPF failure or canary pipeline issue**. A new 7-row **Coverage scope** table replaces the old one-line "Coverage this run:" summary and names each traffic class with its observation status (IPv4 TCP, IPv4 UDP, IPv6, QUIC/HTTP3, io_uring, Unix sockets, payloads beyond iov[0]); the io_uring row reflects actual probe attach status (`⚠ partial` when loaded, `✗ not loaded` otherwise) and annotates the enhanced-profile TLS ClientHello peek. New `MetaEvent.RunnerHasIPv6` + `DigestInput.RunnerHasIPv6` + `config.Config.RunnerHasIPv6` (env `COLDSTEP_RUNNER_HAS_IPV6`): when the runner advertises IPv6 connectivity and no IPv6 hooks are loaded (today: detect mode), the headline verdict downgrades ✅ → ⚠️ so it accurately reflects the partial envelope. Above-the-fold visible-line budget bumped 30 → 40 — intentional H1 trade of compactness for honesty.
- **H2 — Ringbuffer drop counter propagation in JSONL + digest (#194).** Silent BPF event loss now has a user-visible signal. New `MetaEvent.DroppedEvents map[string]uint64` (JSON `dropped_events,omitempty`) populated on the shutdown `MetaEvent` with per-channel counters (`connect`, `udp`, `dns`, `http`, `tls`, `exec`, `fork`, `fs`, `ktls`, `bpf_audit`, `tcp_state`, `io_uring`, `deny`). Only non-zero counters are emitted (constructed via the new `buildDroppedEventsMap` helper — zero keys are dropped before marshal, not at marshal time, so JSON is genuinely free of `"foo": 0` clutter). The detect-digest KPI row is renamed `**⚠️ Ringbuf drops (detect-path total)**` → `**⚠️ Dropped events (ringbuf overflow)** | N` to read cleanly. A single non-zero reserve-failure across any detect-path channel flips the headline badge to ⚠️ (existing `review := …` trigger). The shutdown `MetaEvent` is emitted from the first-registered (LIFO-last) defer in `Run`, so it lands after every per-CPU reserve-failure counter has been snapshotted — no read race.
- **H5 — Structured coverage telemetry stub in MetaEvent (#195).** Machine-readable twin of H1's Coverage scope table. New `telemetry.CoverageReport` with six locked-shape fields embedded as `*CoverageReport` under JSON key `coverage` on `MetaEvent` (omitempty): `ipv4_tcp` / `ipv4_udp_sendmsg` always `true` (gated by always-wired cgroup hooks); `ipv6` / `quic_http3` always `false` in v0.2.9 (probes not yet implemented — keys shipped now, no omitempty, so downstream tooling can rely on schema stability when those probes land); `tls_sni_full` reports `"full"` when the `tls_sni` feature gate is on and the `raw_tp/sys_enter` TLS sniff hook attached cleanly, `"none"` otherwise (`"partial"` reserved for future probe variants); `io_uring` `true` when `raw_tp/io_uring_submit_sqe` attached this run. New `buildCoverageReport` helper composes the report from existing BPF status slice + feature-gate state + `ioUringRd.R` attach signal. No digest changes — H1 already renders the human-facing table, H5 adds the structured form alongside.
- **P6 Phase 2 — io_uring SQE TLS ClientHello peek (enhanced profile) (#191).** Extends the v0.2.8 Phase 1 `raw_tp/io_uring_submit_sqe` probe with a bounded `bpf_probe_read_user` peek at the SQE's user-space buffer to detect TLS ClientHello prefixes (6-byte TLS record signature — effectively zero false-positive rate against random data). Gated behind `COLDSTEP_DETECT_PROFILE=enhanced` via a new `io_uring_peek_cfg` array map; the standard-profile fast path stays unchanged (single CO-RE opcode read). New `io_uring_tls_hello_observed` PERCPU array + `IOUringSendEvent.HasTLSHello bool` + `Summary.IoUringTLSHelloObserved int`. The 40-byte `io_uring_send_event` wire size is preserved by repurposing the trailing `_pad` byte as `has_tls_hello` (the `_Static_assert` and ABI guard test continue to pass). Surfaces as a high-signal **🚨 io_uring TLS ClientHello prefixes** digest row + narrative bullet — non-zero count is high-confidence evidence of TLS-over-io_uring egress that escapes syscall-based hooks. SNI extraction from the record header peek is still out of reach; `IORING_OP_WRITE` / `IORING_OP_WRITEV` remain deferred pending fd→socket resolution.

### Fixed
- **H4 — DNS allowlist startup logging + JSONL surface for partial resolves (#196).** The defend allowlist DNS compile path is no longer silent on partial success. `internal/policy/allowlist.go` emits a single structured `slog.Info("allowlist compiled", "domains", N, "resolved_ips", M, "unresolved", [...])` line at compile time. `MetaEvent` gains two optional fields, `allowlist_ip_count` and `unresolved_domains` (both `omitempty`; zero / nil in detect mode leaves the JSONL header shape unchanged for detect runs). `DigestInput` mirrors them as `AllowlistIPCount` + `UnresolvedDomains`. New `writeUnresolvedDomainsWarning` renders a single GFM blockquote `> ⚠️ **Allowlist: N domain(s) unresolved at startup** — domain1, domain2` at the top of the defend section when `UnresolvedDomains` is non-empty (singular/plural handled; domain names sanitized through the existing `sanitizeCell` Markdown-injection guard). The empty-effective-allowlist case already errored loudly at startup; H4 covers the **partial resolve** case (agent starts cleanly with `M` IPs and `K` unresolved domains, silently-shrunk allowlist denies traffic the operator believed they had allowed — the only prior signal was the cgroup `deny` event after the fact).

### Documentation
- **H6 — IPv4-only coverage boundaries stated in consumer-facing docs (#192).** The defend cgroup programs are bound to `connect4` / `sendmsg4` by kernel ABI; IPv6 egress is **not** observed by coldstep tracepoints and **not** gated by coldstep cgroup hooks until parallel `connect6` / `sendmsg6` hooks and an `allowed_ipv6` LPM trie are implemented. Three consumer surfaces previously implied otherwise and are now corrected: **`action.yml`** `mode:` description now names the actual hook scope (IPv4 TCP/UDP via cgroup `connect4`/`sendmsg4` + BPF LSM where available) and the explicit non-coverage list (IPv6, QUIC/HTTP3, Unix sockets, io_uring connect); **`README.md`** At-a-glance detect/defend rows are qualified ("IPv6 and QUIC not observed", "IPv4 TCP/UDP only — IPv6 and QUIC not blocked") and the Requirements **IP versions** row no longer claims `Defend is IPv4 + IPv6`; **`QUICK_START.md`** replaces the incorrect "IPv6 observe-only" callout with a `> Coverage scope` blockquote linking to `SECURITY.md`. New `SECURITY.md` `## Coverage Boundaries` section adds a per-traffic-class matrix (IPv4 TCP, IPv4 UDP sendmsg, IPv4 UDP write-on-connected, IPv6, QUIC/HTTP3, TLS via io_uring, AF_UNIX, DinD, service containers), a **Why IPv4-only** paragraph explaining the BPF `cgroup/connect4` / `cgroup/sendmsg4` kernel ABI, and operational implications for `suggested-allow` promotion, defend gaps, and sibling-namespace containers.

---

## [v0.2.8] — 2026-05-20

### Added
- **P3-2b — Kernel-confirmed TCP handshake outcomes (#182).** New `tp/sock/inet_sock_set_state` tracepoint emits a `tcp_state_event` (48-byte ABI, `_Static_assert`-locked) on every outbound `SYN_SENT → *` transition, so userspace can split `ESTABLISHED` from `CLOSE` (RST / unreachable / timeout) as the kernel saw it. Complements the syscall-time `connect_event` recorded by P3-2 (#175). The detect digest's `tcp` KPI row picks up a `(confirmed)` suffix plus an `↳ established / refused` sub-row whenever state-machine events arrive in the run; absent state events leave the existing row untouched. Best-effort attach: kernels missing the tracepoint or CO-RE BTF record a non-OK BPFStatus row and the rest of the trace path keeps working.
- **P4 — KTLS offload wired into TLS SNI confidence (#183).** Forward-only fix: the new `ktlsTracker` (`internal/agent/agent_linux_ktls_tracker.go`, `sync.Mutex` + `map[ktlsKey]time.Time`, 60 s TTL) flags any pid that emitted a P3-1 KTLS `setsockopt(SOL_TLS,…)` ringbuf event so subsequent TLS SNI events on that socket are pinned to `TLSConfidenceUnknown` with a new `ConfidenceReason="ktls"` JSONL field (`omitempty`, backwards-compatible). The digest's TLS confidence KPI splits the `unknown` bucket as `unknown=K (N ktls-offloaded)`, the per-row Confidence column renders `unknown ⚠ ktls`, and a Technical-details paragraph points operators at the `ktls_offload` events for cross-reference. Because `tls_sniff_event` does not carry `fd` on the wire today, the ring reader uses a per-pid wildcard lookup with an earliest-mark gate (see #186 below) — exact `(pid, fd)` lookup is preserved for a future BPF ABI bump.
- **P5 — IPv6 TLS SNI sniff (#187).** The TLS ClientHello sniff path now wires through `AF_INET6` connects so IPv6 destinations stop landing with `confidence=unknown` because of a missing `(tgid, fd)` tuple. BPF side: `connect4_tuple` grows 8 → 24 bytes (adds `daddr6[16]` + `is_ipv6`) and `tls_sniff_event` grows 292 → 312 bytes by **appending** a 20-byte IPv6 trailer after `payload` — the legacy IPv4 wire layout (offsets 0..289) stays byte-identical so older replay tooling keeps working. New `coldstep_parse_ipv6_sockaddr24` + `handle_tcp_obs_connect6` hooks fire on every `NR_CONNECT` and bail on family mismatch. Go side: `decodeTLSSniffEvent` returns `daddr6` + `isIPv6`, `readTLSRing` renders bracketed `[2001:db8::1]:443` in the digest while JSONL `Dst` carries the unbracketed form, and `TLSEvent.IsIPv6` marks the row. Defend / HTTP / UDP wire shapes are unchanged this train (IPv4-only by design); explicit guards in `try_emit_buffer_sniff_from_tuple` and `coldstep_tuple_dst_for_fd` skip v6 tuples so the fallback paths don't emit `0.0.0.0` records.
- **P6 Phase 1 — io_uring write-class submission detection (#188).** New `SEC("raw_tp/io_uring_submit_sqe")` handler (kernel 5.14+) filters on `IORING_OP_SENDMSG` (9) and `IORING_OP_SEND` (26) — both unambiguously socket sends — and emits a 40-byte `io_uring_send_event` (`_Static_assert`-locked) on a dedicated 64 KiB ringbuf with per-cpu reserve-failure counter. Surfaces submissions that bypass the syscall hooks used elsewhere in detect mode; the existing `io-uring-disable` sysctl gate remains the primary defense and this is the runtime fallback when the gate is bypassed or off. New `io_uring_send` JSONL event type, `telemetry.Summary.IoUringSendTotal` / `IoUringRingbufReserveFailures` counters, and a KPI row `io_uring writes | N network sends observed (SNI extraction not possible)` (hidden at zero, with a triage-ribbon gap-parts entry and a Technical-details narrative). Attach failure on kernel < 5.14 marks a degraded BPFStatus row and the agent continues. `IORING_OP_WRITE` / `IORING_OP_WRITEV` are deferred to Phase 2 alongside fd→socket resolution.

### Documentation
- **P7 — TLS ECH / SNI extraction limits documented (#181).** Adds a TLS 1.3 Encrypted ClientHello callout to the detect digest's `writeKPISemantics` technical-details fold (gated on `TLSTotal > 0`, placed between the existing KTLS offload note and the io_uring note) explaining that only the outer (CDN / proxy) SNI is visible when ECH is active and pointing operators at DNS HTTPS records / application logs for the true destination. README and QUICK_START gain a new "SNI extraction limits" subsection that gathers the full set of `tls_sni` caveats in one place — fragmented ClientHello (handled by P3-3 inter-syscall reassembly + iov[1] peek), KTLS offload, ECH, and io_uring bypass — so consumers have a single authoritative reference. Two new `digest_test.go` assertions cover the ECH paragraph rendering when `TLSTotal > 0` and being absent at zero.

### Fixed
- **P3 bug audit — 5 fixes + 2 test gaps (#184).** Audit pass over the P3-1/-2/-2b/-3/-4 + P4 code that landed in v0.2.7 / early v0.2.8. (1) **KTLS tracker no longer poisons pre-offload SNI**: each tracker entry now carries a `markedAt` userspace-ringbuf-arrival timestamp and `IsKTLS` takes the TLS event's own `tlsTimestampNs`, so the override only fires when `tlsTimestampNs >= markedAt` — TLS writes that landed before the offload was observed keep their original confidence. (2) **`comm` sanitized in two ring readers**: TCPResult (`connect_result_event`) and KTLS (`ktls_event`) readers were the last paths decoding `comm` via raw `string(bytes.TrimRight(...))`; they now run through `telemetry.SanitizeField(nullTermStr(...), 16)` like every other reader. (3) **`tcp_v4_connect_inflight` HASH → LRU_HASH**: a dropped kretprobe used to wedge a slot in the inflight map for the rest of the process lifetime, silently failing every later connect from the same task with `BPF_NOEXIST`; `BPF_MAP_TYPE_LRU_HASH` lets the kernel evict orphans automatically. (4) **`ktls_events` ringbuf 4 KiB → 64 KiB**: a Go binary using `crypto/tls` + KTLS during dependency fetch can burst dozens of offload events. (5) Plus a minor decoder hardening fix and two new tests (`TestKTLSTracker_PreOffloadTLSPreserved`, decoder sanitization round-trip) that close prior coverage gaps.
- **P3-2b / P4 second bug audit — 5 more fixes (#186).** Second pass focused on the inet_sock_set_state and KTLS→SNI code from #182/#183. (1) **`comm` sanitized in `readTCPStateRing`**: matches every other ring reader, blocks forged JSONL records from processes with newlines or control bytes in `argv[0]`. (2) **KTLS tracker `latest[pid]` clobber fixed**: when two fds on the same pid both offload to kTLS, the old code overwrote `latest[pid]` with the newer `markedAt` so a TLS event arriving between the two Marks gated against the wrong reference and was mis-classified as plaintext; `latest[pid]` now stores the *earliest* surviving Mark while the per-`(pid, fd)` map keeps last-write-wins. (3) **TCPState reader errors now counted**: `readTCPStateRing` `rd.Read()` and decode errors are surfaced via new `TCPStateReaderErrors` on `DigestInput` and a `tcp_state_events reader errors` digest row, so they reach the degraded-hook verdict. (4) **Narrow `refused` classification**: `refused := !confirmed` was over-counting intermediate kernel states (`SYN_RECV`, `FIN_WAIT*`, `LAST_ACK`, `CLOSING`) as refusals; now scoped to `CLOSE | CLOSE_WAIT | TIME_WAIT` via a table-driven `classifyTCPStateTransition()`. (5) Docs-only: `tcp_state_event` `sport` / `dport` comments in `bpf/trace_connect_obs.h` corrected to `host byte order — kernel pre-converts` (the wire decoder already reads little-endian).

---

## [v0.2.7] — 2026-05-19

### Added
- **P3-1 — KTLS offload detection.** New `raw_tp/sys_enter` probe (`bpf/trace_ktls.bpf.c`) filters on the `setsockopt(2)` syscall NR and emits a ringbuf record (`{tgid, tid, comm[16], fd, direction}`) on every `setsockopt(SOL_TLS, TLS_TX|TLS_RX, …)` call. When KTLS is in use the kernel takes over TLS encryption and the existing userspace ClientHello sniffer only ever sees ciphertext fragments — silently producing low-confidence or zero SNI rows — so this probe makes the structural gap visible. New JSONL event type `ktls_offload` (`telemetry.KTLSEvent` / `EventTypeKTLS`) is signed through the existing Signer path; `telemetry.Summary` gains `ktls_offload_events` and `ktls_ringbuf_reserve_failures` counters; the digest surfaces a `KTLS offload | N sockets · SNI extraction not possible` KPI row (hidden at zero) plus a Technical-details note. The setsockopt syscall NR differs per arch (x86_64=54, arm64=208) so the BPF source uses the `bpf_target_x86` / `bpf_target_arm64` macros and the `tracektls` generator goes through `internal/bpf/bpfgen` to set `-D__TARGET_ARCH_<runtime.GOARCH>`. (#174)
- **P3-2 — TCP connect result events.** Paired kprobe/kretprobe on `tcp_v4_connect` captures the kernel return code (0 on success, negative errno otherwise) and emits a supplementary `tcp_result` JSONL event correlated by `pid_tgid`. The detect digest now renders a **TCP connections** KPI row that splits attempts by outcome — e.g. `18 established · 3 refused · 1 timeout · 2 unreachable` — instead of conflating timed-out / refused connections with established ones. Failure to attach the kretprobe is non-fatal; the entry-side `connect_event` is still recorded and the digest falls back to the legacy "attempt" wording with a footnote pointing at the BPF hook status table. (#175)
- **P3-3 — Inter-syscall TLS ClientHello reassembly.** Some TLS stacks (Go `crypto/tls`, `rustls`, Node.js `TLSWrap`) split the ClientHello across two write / writev / sendto syscalls — a 5-byte record header in one and the handshake body in the next. The single-buffer SNI parser only saw the header and the event was dropped as `tls_sni_parse`. A new userspace per-`(pid,dst,dport)` accumulator (`internal/agent/agent_linux_tls_reassembler.go`) stitches the prefix onto a fresh syscall buffer, retries the SNI parse, and emits the `TLSEvent` with `tls_confidence=partial` and a new `ReassembledSNI=true` field when recovery required multiple writes. Bounded to **512 bytes per key**, **30-second TTL**, and a **1024-key LRU**; non-handshake first bytes evict immediately so `application_data` records can't pin memory. Option A (BPF-side per-socket state keyed on `(pid,fd)`) is the long-term path but is deferred in favor of this userspace fallback for the common header/body split. (#173)
- **P3-4 — Per-row TLS SNI confidence column in the digest.** Additive follow-up to P1-4 (#163), which introduced `telemetry.TLSConfidence`, `ScoreTLSConfidence`, and the aggregate `tls SNI confidence` KPI row. This change makes the per-event tier visible **per row** of the TLS section so operators can attribute KPI numbers to specific destinations. `TLSDigestRow` carries a new `Confidence` field; `writeTLSSection` renders a `Confidence` column that defaults to `unknown` when zero-valued; and `writeKPISemantics` Notes block now includes a 4-tier legend (`full` / `partial` / `inferred` / `unknown`) with the `TLSSNIMaxLen=255` boundary that drives the `full → partial` transition. The legend is gated on `TLSTotal > 0` to match the existing KPI-row gate from P1-4. (#178)

---

## [v0.2.6] — 2026-05-19

### Security
- **P2-1 — IPv6 Phase 2 (defend enforces).** Defend mode now blocks non-loopback IPv6 egress not on the allowlist. A new `allowed_ipv6` LPM trie (4-byte prefixlen + 16-byte addr) is programmed at startup from AAAA resolutions; `cgroup/connect6` and `cgroup/sendmsg6` consult it and deny with `EPERM` on miss. `::1` (loopback) and `fe80::/10` (link-local) always bypass enforcement. Allowlist compile resolves A **and** AAAA per domain — AAAA-only services are now valid defend allowlist entries. The digest pivots verdict on a new `DefendIPv6AllowlistSize` field: defend + populated + IPv6 observed → ✅ gated; defend + empty + IPv6 observed → 🚨 block-all (with operator guidance). README "IP versions" cell and `action.yml` input docs reflect the dual-stack flow. (#164)
- **P1-1 — DNS allowlist trust model hardening.** Six new telemetry + digest surfaces around the resolved allowlist: end-of-compile `slog.Info` with domain + resolved-IP counts; `MetaEvent.AllowlistIPCount`; per-unresolved-domain warnings + defend-mode aggregate "Allowlist trust model" digest box; compile-time snapshot recorded on `runStats` with a >5 min age info note ("DNS TTLs may have expired"); `scoreWildcardRisk` flagging entries against known multi-tenant shared-infra suffixes (`s3.amazonaws.com`, `githubusercontent.com`, `cloudfront.net`, `blob.core.windows.net`, `azureedge.net`, `r2.dev`, `pages.dev`) with a dedicated digest box; thread-safe per-domain contact counts rendered as a collapsible digest section sorted descending. (#159)
- **P1-2 — Learning-mode poisoning mitigations.** Four post-run gates so a supply-chain compromise landing during the detect recording window cannot silently bake itself into the allowlist. `build-model --min-observation-hours` / `COLDSTEP_MIN_OBS_HOURS` records `observation_hours` + `short_observation_window`; `assert-integrity` hard-fails when the window is shorter than the gate. `model.BuildSuspiciousDomains` flags high-entropy leftmost DNS labels (Shannon > 3.5 bits/char on labels longer than 8 chars), single-observation rare destinations, and port anomalies (HTTP/TLS observation outside `{22, 25, 53, 80, 443, 465, 587, 3000, 8000, 8080, 8443}`); `render-summary` surfaces a `[!WARNING]` block with the per-reason breakdown. `diff --fail-on-new-domain` exits non-zero with a `::error` annotation when previously-unseen FQDNs appear in the current run (bare-IP destinations excluded to avoid IP-rotation churn). New `QUICK_START.md` section "Promoting detect observations to an allowlist" documents the recommended ≥3 builds → strict diff → review → 24h gate workflow. (#160)
- **P1-3 — eBPF safety audit.** Systematic audit of every BPF C translation unit and the Go-side attach sequence: 24 map-lookup null checks, 11 ringbuf reserve/discard pairs, 4 pointer-arithmetic bounds, 1 loop bound (`SENDMMSG_EXTRA_MAX=7`), 5 BTF CO-RE field stability sites, 26 helper-return checks, 1 cgroup attach cleanup, and 2 allowlist TOCTOU sites — all verified clean. One defense-in-depth hardening landed in `bpf/trace_lsm_defend_lsm.inc:lsm_socket_connect`, which now explicitly zero-initializes `family`/`sk`/`protocol`/`daddr`/`dport` so a probe miss fails closed on pre-5.5 kernels, matching the sibling `lsm_socket_sendmsg` path. Inline `AUDIT(5x)` comments make the review visible at every call site. (#162)
- **P1-4 — SNI confidence scoring for TLS events.** New `TLSConfidence` enum (`full` / `partial` / `inferred` / `unknown`) and a `Confidence` field on `telemetry.TLSEvent`. Scoring rule today is a pure function of the parsed SNI string: empty → `unknown`; `len(sni) >= 255` (RFC 1035 boundary) → `partial`; otherwise → `full`. The detect digest gains a `tls SNI confidence` row inside the Full KPI fold (`full=N · partial=M · unknown=K`), gated on `TLSTotal > 0`. `Confidence` is JSON-`omitempty` so older artifacts and report builders continue to work unchanged. (#163)
- **P1-5 — JSONL injection hardening.** New `telemetry.SanitizeField(s, maxLen)` strips C0 controls (U+0000–U+001F), DEL (U+007F), and C1 controls (U+0080–U+009F); replaces invalid UTF-8 with U+FFFD via `strings.ToValidUTF8`; truncates to a per-field byte budget and walks back to a valid UTF-8 rune boundary. Applied at the single decode point per event type (`agent_linux_ring_read.go` for exec / proc_fork / fs_event / tcp / udp / tls / http / bpf_audit, plus the defend deny path) so attacker-controlled `argv[0]`/`comm`, DNS labels, SNI, or HTTP request prefixes can never splice a forged record into `.coldstep-events.jsonl`. Field budgets honor `TASK_COMM_LEN` (16), `PATH_MAX` (4096), RFC 1035 FQDN (253), and longest HTTP verb + slack (16). New unit + fuzz tests assert JSON-marshalability, no surviving control bytes, byte-budget honored, valid UTF-8. (#161)
- **P0-1 — IPv6 egress detection (Phase 1, observe-only).** Two new BPF programs (`cgroup/connect6`, `cgroup/sendmsg6`) plus matching per-cpu counters surface IPv6 destinations that previously produced zero JSONL events and zero digest warnings. `verdictEmoji` flips to ⚠️ on any non-zero IPv6 counter in detect mode and to 🚨 in defend mode (the IPv4-only allowlist could not gate the connection). `::1` (loopback) is filtered in BPF so localhost IPv6 traffic doesn't dominate counters. Graceful degradation: missing programs on older kernels log at `slog.Info`; the IPv4 defend path is unaffected. Superseded by P2-1 (#164) for the defend enforcement half. (#155)
- **`lsm/socket_sendpage` — close the sendfile/splice egress gap on kernel 5.15.** On 5.15, `sendfile(2)` and `splice(2)` reach the socket via `sock_sendpage()` → `security_socket_sendpage()` rather than `sendmsg`, so neither `cgroup/sendmsg4` nor `lsm/socket_sendmsg` could gate them. A new `lsm/socket_sendpage` hook runs the same IPv4 allowlist + ignored-net + deny-event flow, with a `sendpage_observed` PERCPU_ARRAY counter so userspace can see how much sendfile/splice traffic crossed the hook. On kernel 6.8+ the sendmsg path covers `sendfile`/`splice` via `MSG_SPLICE_PAGES`, so the new hook is redundant but harmless. (#157)
- **Pre-check kernel BTF for `socket_sendpage` LSM hook.** The defend loader now probes the kernel's BTF for the `socket_sendpage` LSM type before attempting to load the program, so kernels that ship `CONFIG_BPF_LSM` without the `sendpage` hook degrade cleanly to the IPv4 sendmsg path instead of failing prog_load. (#158)
- **P0-2 / P0-3 — Coverage scope block + ringbuf drop visibility.** Every digest now emits a one-line Coverage statement (`IPv4 TCP/UDP ✓ observed | IPv6 ✗ not observed | QUIC/HTTP3 ✗ not observed | Payloads beyond iov[0]: <state>`) so a ✅ Clean run badge no longer implies complete observation. The `Payloads beyond iov[0]` cell flips to `⚠️ partial` when any BG-01 partial-observe counter fired this run; when partial-coverage signals are present, the heading emits an extra blockquote subtitle steering the reader to the block. Aggregate ringbuf-drop row surfaces in the Full KPI table when the detect-path sum is non-zero. (#154)

### Added
- **P2-2 — QUIC/HTTP3 observation heuristic.** UDP/443 egress was previously logged as a plain `udp` event with no signal that the underlying HTTPS-equivalent path was opaque to the BPF probes (QUIC encrypts at the transport layer; payload sniff is not possible). `internal/agent/quic_candidate.go` adds a userspace heuristic `IsQUICCandidate(dstIP, dstPort)` — fires when `dstPort == 443` AND `dstIP` parses as non-loopback IPv4 — and the UDP ring reader now emits a synthetic `quic_candidate` JSONL line (`telemetry.EventTypeQUICCandidate`) alongside each matching `udp` event. New `QUICCandidateCount` field on `DigestInput`; a `QUIC (port-443 UDP)` KPI row renders when non-zero with `N flows · payload not inspected` wording plus a Technical-details bullet explaining the encryption gap. No BPF C source, no `go generate`, no `telemetry.Summary` field, no `action.yml` input. (#167)
- **P2-3 — Runner compatibility matrix CI + agent startup probe.** New weekly `coldstep-runner-compat.yml` (Mon 04:00 UTC + dispatch) exercises four sibling jobs (`vanilla`, `dind`, `buildkit`, `service-containers`), each `continue-on-error: true` so one broken variant doesn't cancel the rest; an `aggregate` job converts per-variant outcomes into a single pass/fail summary table. Agent-side: `CheckRunnerCompat()` (`internal/agent/compat_check_linux.go`) returns non-fatal `[]telemetry.CompatWarning` with stable codes — `cgroup_v1_detected`, `cgroup_namespace_nondelegated` (detected from `/.dockerenv` + host-prefixed `/proc/1/cgroup`), `deep_cgroup_nesting` (depth ≥ 4), `container_cgroup_detection_failed`. Warnings are logged via `slog.Warn("runner_compat_warning", ...)` and surfaced in `telemetry.Summary.CompatWarnings`. Always non-fatal: detect mode proceeds regardless. (#166)
- **P2-4 — Kernel coupling regression harness + BTF startup probe.** `internal/agent/btf_probe.go` wraps `btf.LoadKernelSpec` with a named, actionable error (`kernel BTF unavailable (requires kernel 5.5+ with CONFIG_DEBUG_INFO_BTF=y): ...`); the agent calls it early in `Main` and aborts startup with `slog.Error(..., "btf_available", false, ...)` on failure rather than letting CO-RE relocation fail cryptically at `prog_load`. The positive signal surfaces on a synthetic `{Name: "btf", OK: true, BTFAvailable: true}` row in `bpfSt` so `.coldstep-telemetry.json` parsers that already iterate the `bpf` array see it for free. New `BTFAvailable` field on `telemetry.BPFStatus` (omitempty). New `.github/workflows/coldstep-kernel-matrix.yml` workflow (weekly Mon 03:00 UTC + dispatch) runs build + unit + integration over a 5.15 / 6.1 / 6.6 / 6.8 matrix — today every row runs on the host `ubuntu-latest` kernel; the workflow is documented as the scaffold a future qemu/vmtest harness drops into. The reusable CI runner additionally annotates a warning when the runner kernel drifts >2 majors past the 5.15 floor. (#165)
- **P2-5 — Reputation integration hooks.** Stable, extensible plug-in interface under `internal/reputation` so external integrators can ship IP-reputation enrichers without modifying core coldstep. `Enricher` interface, `Result` struct, `NoOpEnricher`, `Register`/`Registered`/`EnrichAll` with concurrent fan-out, deterministic name-sorted output, and ctx-deadline guard so a hung backend cannot block its peers. Ships an OTX backend (`X-OTX-API-KEY`, 10s timeout, 4 MiB JSON cap, 0–10 score derived from pulse count) and a CIRCL PassiveDNS stub. `internal/reputation/loader` is env-driven (`COLDSTEP_OTX_API_KEY`, `COLDSTEP_VIRUSTOTAL_API_KEY`, `COLDSTEP_PASSIVEDNS_SERVER`); empty env vars install a `NoOpEnricher` so the returned slice shape stays stable. `coldstep-report` `rdns-enrich` and `otx-enrich` invoke the loader after their existing work, writing combined results into `m["reputation_results"]` plus an `m["reputation"]` summary. Enrichment is **post-processing only** — runs after JSONL is on disk, never on the agent's hot path. Coexists with the legacy `OTX_API_KEY` flow (different env var). (#168)

### Changed
- **Compact GFM-compliant Step Summary digest.** Job Summaries render GitHub Flavored Markdown (standard Markdown plus a narrow HTML subset: `<details>`, `<summary>`, `<br>`, `<hr>`, `<table>`); anything outside that subset rendered as literal text. The previous digest used `<font color="">`, `<p align="center">`, `<sub>`, and `<center>` — all four are now gone. The visible portion of the digest is roughly 19 lines for a typical detect run (test-enforced budget: 30): a `## <emoji> coldstep — <mode>` headline, a one-row 5-column KPI table, top-10 destinations, a triage table with only rows that have something to say, and a footer pointer to `.coldstep-events.jsonl`. Everything else (full per-channel KPI table, defend section, run info, per-protocol event tables, BPF hook status, KPI semantics) lives inside a single `<details>` "Technical details" fold; each per-protocol event table is its own nested `<details>` so operators can drill into one protocol without scrolling past the rest. Pure presentation change; no `DigestInput` / `telemetry.Summary` shape change, no agent or BPF code touched. (#152)
- **Complete PERCPU_ARRAY migration for hot-path counters.** Seven observability counters were still `BPF_MAP_TYPE_ARRAY` + `__sync_fetch_and_add` despite the project's documented convention (PERCPU_ARRAY for no global atomic contention): `udp_sendmsg_multi_iovec_observed`, `tls_writev_multi_iovec_observed`, `unobserved_egress_syscalls_observed`, `io_uring_setup_observed`, `dns_recvfrom_buf_update_failures`, `tcp_dns_responses_observed`, `tcp_dns_skipped_short_read`. Converted to `BPF_MAP_TYPE_PERCPU_ARRAY` with plain `(*v)++` on hot syscall paths (every UDP sendmsg, io_uring_setup, TLS writev, DNS recvfrom). Go reader swapped six callers from `readUint32CounterMap` → `readUint32PerCPUArraySum`; deleted the now-unused scalar helper and its tests. Not a correctness bug — `__sync_fetch_and_add` is atomic-safe — but eliminates cache-line bouncing on hot paths. Also pins `securego/gosec` to a SHA (`v2.26.1`). (#139)
- **Go Tier-1 simplifications (~219 LOC removed).** Pure refactor across agent, report, policy, config, and action layers — no behavior change. Drops 17 thin BPF counter-read wrappers (`readDenyReserveFailureCount` and friends) that only nil-checked `*Objects` and forwarded to existing helpers. Factors `loadDefendMaps` + `loadLSMDefendMaps` into one `loadDefendMapsForBackend` parameterised by a `defendMapSet`. Collapses four identical `*EmptyReason` digest helpers into a single `sectionEmptyReason(degraded, readerErrors)`. Dedupes comma + whitespace token splitters in `internal/policy/ignore.go` and `cmd/coldstep-action/allowlist_files.go`. Consolidates domain normalisation into `internal/policy.NormalizeDomainsFromRaw` (`internal/config` deletes its copy). Drops the redundant explicit legacy-mode rejection branch — the "mode not in {detect, defend}" check already covers it. (#141)
- **BG-03 — `sendmmsg` multi-message observation up to `vlen=8`.** `traceconnect` now observes every message in a `sendmmsg(2)` batch up to `vlen=8` instead of only the first; the existing `sendmmsg_first_only` counter still fires when `vlen > 8` so the partial-coverage signal stays accurate. (#153)

### Fixed
- **`io-uring-disable` now actually disables io_uring.** Both `src/start.ts` and `cmd/coldstep-action/main.go` invoked `sudo sysctl -w io_uring_disabled=2`, but sysctl rejects the bare key with `error: "No such file or directory"`. The correct fully-qualified key is `kernel.io_uring_disabled`. The Go path also swaps the silent `_ = exec.Command(...).Run()` discard for `CombinedOutput()` + a GitHub Actions `::warning::` annotation on failure, so future breakage (older kernels without the sysctl, sandboxed runners) is visible in the job log instead of swallowed. (#151)

---

## [v0.2.5] — 2026-05-18

### Added
- **HTTP/1 sniff parity for write(2) family** — plaintext HTTP/1 requests on dport 80 are now captured when emitted via `write(2)`, `writev(2)`, `pwrite64(2)`, `pwritev(2)`, or `pwritev2(2)` on a connected TCP socket. Previously only the `sendto(2)` arm produced `http_events`; Go `net/http`, libcurl over plain HTTP, and most stdlib HTTP clients use `write(2)` and were silently missing from the HTTP JSONL stream. The new dispatch helper fetches the (tgid,fd) tuple once and runs both the TLS-ClientHello sniff and the HTTP/80 sniff from a single LRU lookup, keeping the verifier budget flat. (BG-04)

### Changed
- **Detect digest redesign** — the `.coldstep-detect.md` / Job Summary digest now leads with a one-line headline status badge (🚨 Alert / ⚠️ Review / ✅ Clean run), surfaces the hot-egress table before the triage ribbon, reorders the KPI table to network → process → fs → health with ringbuf-reserve and multi-iovec rows directly under their parent metric, and collapses the long-form KPI prose / standalone BPF hook table / Footnotes block into a single `<details>` "Technical details" fold plus a small Run info table. ⚠️ (with VS-16) is now used consistently for warning rows. Pure presentation change; no `DigestInput` / `telemetry.Summary` shape change, no agent or BPF code touched. (#149)
- refactor: consolidate four `*EmptyReason` digest helpers into `protocolEmptyReason`.
- **BPF telemetry — per-syscall partial-observe counters (BG-01).** Replaced the dead aggregate `unobserved_egress_syscalls_observed` ARRAY (no increment sites since the pwrite* sniff arms were added; always read 0) with a 4-slot `partial_egress_observed` PERCPU_ARRAY. Slots: `sendfile`/`sendfile64`, `splice`, and `sendmmsg` (first-message-only). The digest, triage ribbon, and telemetry JSON now surface each path independently (`sendfile_observed`, `splice_observed`, `sendmmsg_first_only`) so operators can see *which* syscall drove the visibility gap instead of a single total. Existing sniff paths are unchanged.
- **BG-02: bounded multi-iovec sniff** — When `writev`/`pwritev`/`pwritev2` is called with `vlen >= 2`, or `sendmsg`/`sendmmsg` is called with `msg_iovlen >= 2`, the BPF agent now performs a single additional bounded `bpf_probe_read_user` of `iov[1]` and feeds it through the existing TLS ClientHello and HTTP request fingerprint helpers. Previously only `iov[0]` was inspected, and any TLS/HTTP content fragmented into `iov[1]` (e.g. header-then-body framing) was silently dropped from `tls_events` / `http_events`. Strictly capped at index 1 — no unlimited iov walk. The `tls_writev_multi_iovec_observed` and `udp_sendmsg_multi_iovec_observed` counters continue to fire so the historical fragmentation-rate metric stays comparable.
- BG-03: `sendmmsg_multi_message_observed` counter tracks NR_SENDMMSG calls with vlen>1 (separate from multi-iovec counter)

---

## [v0.2.4] — 2025-05-16

### Fixed
- **cgroup root-attach** — BPF programs now attach to `/sys/fs/cgroup` (the cgroup v2 unified-hierarchy root) instead of a sub-cgroup derived from `/proc/self/cgroup`. On GitHub-hosted `ubuntu-latest` runners, job-step processes run in sibling or parent cgroups relative to the coldstep agent (which executes via `sudo`). Attaching to a sub-cgroup left those steps unprotected — the root cause of defend-mode blocking zero connections despite BPF programs loading successfully. Fixes #122.

### Changed
- **Runtime SHA256 verification** — removed the hardcoded `COLDSTEP_BINARY_SHA256` constant from `src/shared.ts`. Go binaries are not reproducible across CI runs, so a hardcoded SHA drifted after every `supply-chain-attest` build. The pre-step now fetches the expected digest at runtime from the GitHub Releases API (`asset.digest` on `GET /repos/coldstep-io/coldstep/releases/tags/{version}`), eliminating the mismatch cycle.

### Verified
- Defend-mode lab: 0 of 3 attack simulations reached the signal server (env exfil, DNS exfil, credential scan — all blocked).

---

## [v0.2.3] — 2025-05-15

### Notes
- Intermediate release tag. Binary predates the cgroup root-attach fix; dist bundles carried a stale SHA. Use v0.2.4.

---

## [v0.2.2] — 2025-05-14

### Notes
- Intermediate release tag. Dist bundles pointed at wrong SHA. Use v0.2.4.

---

## [v0.2.1] — 2025-04-30

### Added
- Initial public release.
- Linux eBPF agent with eight BPF probe packages: `traceexec`, `tracefork`, `traceconnect`, `tracedefend`, `tracelsmdefend`, `tracedns`, `tracefs`, `tracebpfaudit`.
- Two runtime modes: `detect` (observe-only telemetry) and `defend` (cgroup `connect4`/`sendmsg4` + BPF LSM `socket_connect`/`socket_sendmsg` for IPv4 egress blocking). The spelling `enforce` is explicitly rejected at startup.
- `coldstep-action` binary (`start`/`stop`) for composite Action lifecycle management.
- `coldstep-report` post-run pipeline: `build-model`, `assert-integrity`, `render-summary`, `render-html`, `diff`, `rdns-enrich`, `otx-enrich`, `render-ip-summary`.
- Workspace artifacts: `.coldstep-events.jsonl` (append-only event stream), `.coldstep-detect.md` (Markdown shutdown digest), `.coldstep-telemetry.json` (BPF health totals), `.coldstep-ready.json` (readiness probe).
- Optional AlienVault OTX enrichment for detect HTML reports when `OTX_API_KEY` is set; skipped silently when unset.
- Composite GitHub Action (`action.yml`) with inputs: `mode`, `allow`, `allow-file`, `detect-profile`, `report`, `fail-on-error`, `signing-key`, `log-level`, `ignored-nets[-file]`, `bootstrap-allowlist`, `ready-timeout-seconds`, `github-token`.
- CI gates: `gofmt`, encoding scan, unit tests (amd64 + arm64 matrix), integration tests (root + BPF), action bundle, detect-mode and defend-mode lab runs.
- BPF C unit tests (`bpf/host_test/`) via `scripts/run-bpf-c-unit-tests.sh`.
- `COLDSTEP_DETECT_PROFILE=enhanced` enables `proc_tree`, `tls_sni`, and `fs_events` streams by default.

---

[0.5.2]: https://github.com/coldstep-io/coldstep/releases/tag/v0.5.2
[v0.5.1]: https://github.com/coldstep-io/coldstep/releases/tag/v0.5.1
[v0.5.0]: https://github.com/coldstep-io/coldstep/releases/tag/v0.5.0
[v0.4.1]: https://github.com/coldstep-io/coldstep/releases/tag/v0.4.1
[v0.4.0]: https://github.com/coldstep-io/coldstep/releases/tag/v0.4.0
[v0.3.0]: https://github.com/coldstep-io/coldstep/releases/tag/v0.3.0
[v0.2.9]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.9
[v0.2.8]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.8
[v0.2.7]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.7
[v0.2.6]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.6
[v0.2.5]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.5
[v0.2.4]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.4
[v0.2.3]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.3
[v0.2.2]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.2
[v0.2.1]: https://github.com/coldstep-io/coldstep/releases/tag/v0.2.1
